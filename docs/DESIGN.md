# DESIGN — 게임 설계 (GDD)

> 확정분만 기록한다. 스튜디오 📄 문서 탭에서 열람.

## 화면 구성과 플로우

| 화면 | 내용 | 전환 |
|---|---|---|
| 타이틀 | 로고(알바킹) + "출근하기" 버튼 + 태그라인. 버튼 1개 원칙 — 클릭 3초 내 인게임 | 출근하기 → 게임 루프 |
| 게임 루프 | 지시어(0.8초, 화면 중앙 대형) → 플레이(5~10초) → 판정 연출(0.6초) → 다음 레벨 | 목숨 0 → 게임오버 |
| 게임오버 | "근무 종료!" + 점수 + 사장님 평가서(P6: LLM, 폴백 뱅크) + AI 스태프롤 3줄 | 재출근 → 게임 루프 |

- 레벨 배치: 테마 교차 순환(같은 템플릿 연속 금지), 3레벨 클리어마다 난이도 단계 상승("난이도 상승 — 새 변형 투입" 배너).
- 목숨 3개, 실패해도 다음 레벨로 전진(점수만 손해). 콤보: 연속 성공 카운터.
- 점수: 클리어 100 + 남은 시간 보너스. 퍼펙트(제한시간 40% 이상 남김) 시 가산.

## 캔버스·조작 규격 (확정)

- **논리 해상도 720×1280 (9:16 세로)** — 엔진 좌표계는 논리 px. CSS transform scale로 화면에 맞춤, 데스크톱은 중앙 정렬.
- 입력: Pointer Events 단일 경로(탭/드래그 공용). `touch-action: none`.
- 루프: 고정 타임스텝 60Hz 누산기 — 엔진 tick은 순수 함수(렌더 무관·시드 RNG) → 봇 QA(P5)가 같은 tick을 호출.

## 테마별 게임 진행 (10종)

| # | 알바 | 목표 | 조작 | 실패 | 난이도 파라미터 |
|---|---|---|---|---|---|
| 1 | 초밥집 | 참치를 샤리 위에 올린다 | 재료를 드래그해 드롭존에 | 시간 초과 | 개수 1→3, 시간 ↓, 페이크 재료 등장 |
| 2 | 김밥천국 | 재료를 김밥 위에 올린다 | 〃 | 시간 초과 | 재료 수 ↑, 지정 순서 모드 |
| 3 | 편의점 | 상품을 스캐너에 찍는다 | 상품을 스캐너로 드래그 | 시간 초과 | 상품 수 ↑, 지정 상품만 찍기 |
| 4 | 빵집 | 주문 빵을 트레이에 담는다 | 빵을 트레이로 드래그 | 잘못된 빵/시간 초과 | 주문 수 ↑, 비슷한 빵 섞기 |
| 5 | 카페 | 물을 적정선까지 붓는다 | 홀드로 게이지 채우고 놓기 | 넘침/모자람 | 적정 구간 폭 ↓, 게이지 속도 ↑ |
| 6 | 치킨집 | 노릇할 때 건진다 | 게이지 적정 타이밍에 탭 | 태움/설익음 | 구간 폭 ↓, 속도 ↑, 2망 동시 |
| 7 | 붕어빵 | 타이밍에 맞춰 뒤집는다 | 신호 순간 탭 | 타이밍 이탈 | 판정 폭 ↓, 연속 2~3회 |
| 8 | 택배 상하차 | 박스를 전부 옮긴다 | 연타 | 목표 미달 | 목표 횟수 ↑, 시간 ↓ |
| 9 | 세차장 | 거품을 다 닦는다 | 스와이프로 문지르기 | 잔여 거품 | 거품 면적 ↑, 시간 ↓ |
| 10 | 노래방 | 흥을 채운다 | 리듬 연타 | 게이지 미달 | 목표 ↑, 감쇠 속도 ↑ |

## 레벨 JSON 스키마 (초안)

```json
{ "id": "sushi-d2-01", "template": "drag", "theme": "sushi", "difficulty": 2,
  "timeLimit": 4800, "params": { "itemCount": 2, "fakeItems": 1 } }
```

사다리 풀(P5): Claude가 대량 생성 → 봇 전수 검증·서열화 → 적응 선택기가 스킬 점수에 맞는 레벨을 선택(ADR-001).

## 텔레메트리 (게임오버 평가서 입력, P6)

테마별 {시도, 성공, 퍼펙트, 평균 클리어 시간, 실패 유형}, 최고 콤보, 최다 실패 테마 — 로컬 수집만, 외부 전송 없음.

## 확정

- 장르: 알바 체험 마이크로게임 연발 (레벨당 5~10초, 즉시 이해 가능한 한 단어 지시어)
- 실패 처리: 목숨제 + 점수제 (실패해도 전진 — 심사자 콘텐츠 노출 극대화)
- 난이도: EWMA 스킬 점수 + 목표 성공률 70~80% 제어기 → 사전 검증된 사다리 풀에서 선택 (ADR-001)
- 템플릿 3종: A 드래그&드롭 / B 타이밍 탭·게이지 / C 연타·스와이프 — 템플릿당 스킨 3~4개
- 코어 규칙: 게임 로직은 렌더와 분리 (순수 TS 모듈 · 고정 타임스텝 · 시드 RNG) — 봇 QA·스튜디오 프리뷰의 전제
- 화면: 세로형 중심 반응형 캔버스, 데스크톱 중앙 정렬, 탭/드래그 공용 조작
- 텍스트: 게임 내 모든 카피는 `pipeline/texts.json` 단일 소스 — 이미지에 굽지 않고 코드로 렌더, 스튜디오에서 검수

## 미니게임 라인업 10종 (확정)

| # | 알바 | 템플릿 | 지시어 | 인터랙션 | 핵심 에셋 |
|---|---|---|---|---|---|
| 1 | 초밥집 | A 드래그 | 올려! | 재료를 샤리 위로 드래그 | 배경·샤리·참치·접시 |
| 2 | 김밥천국 | A 드래그 | 넣어! | 재료를 김밥 위로 드래그 | 배경·김밥면·햄·계란·단무지 |
| 3 | 편의점 | A 드래그 | 찍어! | 상품을 스캐너로 드래그 | 배경·스캐너·상품 3종 |
| 4 | 빵집 | A 드래그 | 담아! | 빵을 트레이에 드래그 | 배경·트레이·빵 3종 |
| 5 | 카페 | B 타이밍 | 부어! | 게이지 맞춰 물 붓기 | 배경·컵·주전자 |
| 6 | 치킨집 | B 타이밍 | 건져! | 게이지 맞춰 튀김 건지기 | 배경·기름솥·튀김망·치킨 |
| 7 | 붕어빵 노점 | B 타이밍 | 뒤집어! | 타이밍 맞춰 뒤집기 | 배경·붕어빵틀·붕어빵 |
| 8 | 택배 상하차 | C 연타 | 날라! | 연타로 박스 옮기기 | 배경·박스·트럭 |
| 9 | 세차장 | C 스와이프 | 문질러! | 스와이프로 거품 닦기 | 배경·자동차·스펀지·거품 |
| 10 | 노래방 | C 연타 | 흔들어! | 연타로 탬버린 흔들기 | 배경·탬버린·마이크 |

템플릿 배분: A×4 / B×3 / C×3. 배치 순서는 A-B-C 교차(같은 템플릿 연속 금지). 강화 변형(2개 동시, 순서 지정, 페이크 재료 등)은 사다리 풀에서 파라미터로 정의.

## 미정 (P1에서)

- 캔버스 논리 해상도, 템플릿별 상세 스펙, 레벨 JSON 스키마, 텔레메트리 스키마
